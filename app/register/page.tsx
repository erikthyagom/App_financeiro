export const dynamic = "force-dynamic";

import RegisterClient from "./RegisterClient";

export const metadata = {
  title: "Cadastrar | Controle Financeiro",
};

export default function RegisterPage() {
  return <RegisterClient />;
}
